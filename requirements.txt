# Requirements pour l'Agent IA Local - Mode Decision Tree
# Version allégée sans dépendances LLM/OpenAI

# Interface utilisateur
streamlit>=1.28.0

# Manipulation de données
pandas>=2.0.0
numpy<2.0.0  # Compatibility fix for current environment

# Visualisations
matplotlib>=3.6.0
seaborn>=0.12.0
folium>=0.15.0
streamlit-folium>=0.18.0

# Base de données vectorielle locale
chromadb>=0.4.15

# Utilitaires
python-dotenv>=1.0.0
Pillow>=9.5.0

# Optionnel : pour des fonctionnalités avancées
openpyxl>=3.1.0  # Pour la lecture de fichiers Excel
xlrd>=2.0.1      # Pour les anciens fichiers Excel

# Développement et tests (optionnel)
# pytest>=7.0.0
# black>=23.0.0

# Note: 
# - Toutes les dépendances OpenAI/LangChain ont été supprimées
# - sentence-transformers supprimé (remplacé par SimpleCache)
# - faiss-cpu supprimé (remplacé par SimpleCache basique)
# - NumPy fixé à <2.0.0 pour éviter les problèmes de compatibilité
